# @xebra/web

Next.js (App Router) frontend — corridor selector, unified status dashboard, `/intent/[hash]`
verification pages, wagmi (Arc) + Stellar Wallets Kit (Stellar source signing) + Solana address
field (destination only, no wallet connector).

**STATUS**: not yet scaffolded. Full Next.js app + dependencies land in the frontend phase task
(see `/docs/architecture.md` §9 and phased delivery plan stage 5) — deferred until the
`intent-schema`, `router-core`, and `api` packages it depends on exist.
