/**
 * @xebra/arbiter-signer
 *
 * KMS-backed EVM (secp256k1) + Stellar (Ed25519) signer adapters for the v1 admin arbiter. See docs/architecture.md #8.
 *
 * STATUS: scaffold placeholder — implementation lands in its phase task
 * (see /docs/architecture.md phased delivery plan).
 */

export const XEBRA_PACKAGE = "arbiter-signer";
