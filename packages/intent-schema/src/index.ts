/**
 * @xebra/intent-schema
 *
 * IntentV2 schema, arc-v1 legacy adapter, canonical intent hashing. See docs/architecture.md #1.
 *
 * STATUS: scaffold placeholder — implementation lands in its phase task
 * (see /docs/architecture.md phased delivery plan).
 */

export const XEBRA_PACKAGE = "intent-schema";
